From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Gr1mly4Memes <alagozantoni14@gmail.com>
Date: Sun, 22 Feb 2026 18:03:08 -0800
Subject: [PATCH] Pluto Memory allocation optimisations


diff --git a/src/main/java/org/bukkit/craftbukkit/util/BlockStateListPopulator.java b/src/main/java/org/bukkit/craftbukkit/util/BlockStateListPopulator.java
index 989b2f751a592251aa9f860010fc833a0e5a484d..b08d8a8d6f892f58fd3bc6cf6560a827224da381 100644
--- a/src/main/java/org/bukkit/craftbukkit/util/BlockStateListPopulator.java
+++ b/src/main/java/org/bukkit/craftbukkit/util/BlockStateListPopulator.java
@@ -25,7 +25,29 @@ import org.bukkit.craftbukkit.block.CraftBlockStates;
 public class BlockStateListPopulator extends DummyGeneratorAccess {
 
     private final LevelAccessor level;
-    private final Map<BlockPos, CapturedBlock> blocks = new LinkedHashMap<>();
+    // Papercut
+    private final it.unimi.dsi.fastutil.longs.Long2ObjectLinkedOpenHashMap<CapturedBlock> blocks = new it.unimi.dsi.fastutil.longs.Long2ObjectLinkedOpenHashMap<>();
+    @Override
+    public net.minecraft.world.level.block.state.BlockState papercut$getBlockState(int x, int y, int z) {
+        CapturedBlock state = this.blocks.get(BlockPos.asLong(x, y, z));
+        return (state != null) ? state.state() : this.level.papercut$getBlockState(x, y, z);
+    }
+    @Override
+    public net.minecraft.world.level.block.state.BlockState papercut$getBlockState(long packedPos) {
+        CapturedBlock state = this.blocks.get(packedPos);
+        return (state != null) ? state.state() : this.level.papercut$getBlockState(packedPos);
+    }
+    @Override
+    public FluidState papercut$getFluidState(int x, int y, int z) {
+        CapturedBlock state = this.blocks.get(BlockPos.asLong(x, y, z));
+        return (state != null) ? state.state().getFluidState() : this.level.papercut$getFluidState(x, y, z);
+    }
+    @Override
+    public FluidState papercut$getFluidState(long packedPos) {
+        CapturedBlock state = this.blocks.get(packedPos);
+        return (state != null) ? state.state().getFluidState() : this.level.papercut$getFluidState(packedPos);
+    }
+    // Papercut
 
     private List<CraftBlockState> snapshots;
 
@@ -35,19 +57,19 @@ public class BlockStateListPopulator extends DummyGeneratorAccess {
 
     @Override
     public net.minecraft.world.level.block.state.BlockState getBlockState(BlockPos pos) {
-        CapturedBlock block = this.blocks.get(pos);
+        CapturedBlock block = this.blocks.get(pos.asLong()); // Papercut
         return block != null ? block.state() : this.level.getBlockState(pos);
     }
 
     @Override
     public FluidState getFluidState(BlockPos pos) {
-        CapturedBlock block = this.blocks.get(pos);
+        CapturedBlock block = this.blocks.get(pos.asLong()); // Papercut
         return block != null ? block.state().getFluidState() : this.level.getFluidState(pos);
     }
 
     @Override
     public BlockEntity getBlockEntity(BlockPos pos) {
-        CapturedBlock block = this.blocks.get(pos);
+        CapturedBlock block = this.blocks.get(pos.asLong()); // Papercut
         return block != null ? block.blockEntity() : this.level.getBlockEntity(pos);
     }
 
@@ -55,7 +77,10 @@ public class BlockStateListPopulator extends DummyGeneratorAccess {
     public boolean setBlock(BlockPos pos, net.minecraft.world.level.block.state.BlockState state, @Block.UpdateFlags int flags, int recursionLeft) {
         pos = pos.immutable();
         // remove first to keep last updated order
-        this.blocks.remove(pos);
+        // Papercut
+        final long posLong = pos.asLong();
+        this.blocks.remove(posLong);
+        // Papercut
 
         final BlockEntity newBlockEntity;
         if (state.getBlock() instanceof EntityBlock entityBlock) {
@@ -71,7 +96,7 @@ public class BlockStateListPopulator extends DummyGeneratorAccess {
             newBlockEntity = null;
         }
 
-        this.blocks.put(pos, new CapturedBlock(state, flags, newBlockEntity));
+        this.blocks.put(posLong, new CapturedBlock(state, flags, newBlockEntity)); // Papercut
         return true;
     }
 
@@ -92,10 +117,10 @@ public class BlockStateListPopulator extends DummyGeneratorAccess {
     }
 
     private void iterateSnapshots(Consumer<CraftBlockState> callback) {
-        for (Map.Entry<BlockPos, CapturedBlock> entry : this.blocks.entrySet()) {
+        for (final it.unimi.dsi.fastutil.longs.Long2ObjectLinkedOpenHashMap.Entry<CapturedBlock> entry : this.blocks.long2ObjectEntrySet()) { // Papercut
             CapturedBlock block = entry.getValue();
             CraftBlockState snapshot = CraftBlockStates.getBlockState(
-                this.getMinecraftWorld().getWorld(), entry.getKey(), block.state(), block.blockEntity()
+                    this.getMinecraftWorld().getWorld(), BlockPos.of(entry.getLongKey()), block.state(), block.blockEntity() // Papercut
             );
             snapshot.setFlags(block.flags());
             snapshot.setWorldHandle(this.level);
diff --git a/src/main/java/org/bukkit/craftbukkit/util/DelegatedGeneratorAccess.java b/src/main/java/org/bukkit/craftbukkit/util/DelegatedGeneratorAccess.java
index 323dea1260cb68e43172a10b4514cf29dc5151b8..ff146ba12b335bd25e8c52f336098db7c98e8126 100644
--- a/src/main/java/org/bukkit/craftbukkit/util/DelegatedGeneratorAccess.java
+++ b/src/main/java/org/bukkit/craftbukkit/util/DelegatedGeneratorAccess.java
@@ -779,6 +779,56 @@ public abstract class DelegatedGeneratorAccess implements WorldGenLevel {
     public FluidState getFluidIfLoaded(final BlockPos pos) {
         return this.delegate.getFluidIfLoaded(pos);
     }
+    // Papercut
+    @Override
+    public int papercut$getDirectSignal(int x, int y, int z, Direction direction) {
+        return this.delegate.papercut$getDirectSignal(x, y, z, direction);
+    }
+    @Override
+    public int papercut$getControlInputSignal(int x, int y, int z, Direction direction, boolean diodesOnly) {
+        return this.delegate.papercut$getControlInputSignal(x, y, z, direction, diodesOnly);
+    }
+    @Override
+    public boolean papercut$isEmptyBlock(long packedPos) {
+        return this.delegate.papercut$isEmptyBlock(packedPos);
+    }
+    @Override
+    public boolean papercut$isEmptyBlock(int x, int y, int z) {
+        return this.delegate.papercut$isEmptyBlock(x, y, z);
+    }
+    @Override
+    public BlockState papercut$getBlockState(int x, int y, int z) {
+        return this.delegate.papercut$getBlockState(x, y, z);
+    }
+    @Override
+    public BlockState papercut$getBlockState(long packedPos) {
+        return this.delegate.papercut$getBlockState(packedPos);
+    }
+    @Override
+    public BlockState papercut$getBlockStateIfLoaded(int x, int y, int z) {
+        return this.delegate.papercut$getBlockStateIfLoaded(x, y, z);
+    }
+    @Override
+    public BlockState papercut$getBlockStateIfLoaded(long packedPos) {
+        return this.delegate.papercut$getBlockStateIfLoaded(packedPos);
+    }
+    @Override
+    public FluidState papercut$getFluidState(int x, int y, int z) {
+        return this.delegate.papercut$getFluidState(x, y, z);
+    }
+    @Override
+    public FluidState papercut$getFluidState(long packedPos) {
+        return this.delegate.papercut$getFluidState(packedPos);
+    }
+    @Override
+    public FluidState papercut$getFluidStateIfLoaded(int x, int y, int z) {
+        return this.delegate.papercut$getFluidStateIfLoaded(x, y, z);
+    }
+    @Override
+    public FluidState papercut$getFluidStateIfLoaded(long packedPos) {
+        return this.delegate.papercut$getFluidStateIfLoaded(packedPos);
+    }
+    // Papercut
 
     @Nullable
     @Override
diff --git a/src/main/java/org/bukkit/craftbukkit/util/DummyGeneratorAccess.java b/src/main/java/org/bukkit/craftbukkit/util/DummyGeneratorAccess.java
index 3693c069f91152d27cb550a0f6fbcb270761972f..cbadba33bf3481f2541a3402bc5c612ba28c9aac 100644
--- a/src/main/java/org/bukkit/craftbukkit/util/DummyGeneratorAccess.java
+++ b/src/main/java/org/bukkit/craftbukkit/util/DummyGeneratorAccess.java
@@ -209,6 +209,42 @@ public class DummyGeneratorAccess implements WorldGenLevel {
         throw new UnsupportedOperationException("Not supported yet.");
     }
 
+    // Papercut
+    @Override
+    public BlockState papercut$getBlockState(int x, int y, int z) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+        public BlockState papercut$getBlockState(long packedPos) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+        public BlockState papercut$getBlockStateIfLoaded(int x, int y, int z) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+        public BlockState papercut$getBlockStateIfLoaded(long packedPos) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+        public FluidState papercut$getFluidState(int x, int y, int z) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+        public FluidState papercut$getFluidState(long packedPos) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    @Override
+    public FluidState papercut$getFluidStateIfLoaded(int x, int y, int z) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+
+    @Override
+    public FluidState papercut$getFluidStateIfLoaded(long packedPos) {
+        throw new UnsupportedOperationException("Not supported yet.");
+    }
+    // Papercut
+
     @Override
     public BlockState getBlockState(BlockPos pos) {
         return Blocks.AIR.defaultBlockState(); // SPIGOT-6515
